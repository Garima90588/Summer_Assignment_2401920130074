class Solution {
    public int[] sortedSquares(int[] nums) {
        int[] arr = new int[nums.length];
        int i = 0;

        for(int num : nums) {
            arr[i++] = num * num;
        }

        Arrays.sort(arr);

        return arr;
    }
}
